Storage types:-
================

EBS(elastic block storage) --> block by block 1-16 gb SAN(Storage are network) storage its too cost we can attach to the server whenever we want.

EFS(elastic File System) --> File level storage NAS(network attach storage) Storage cheap compare to ebs and its we can use as much as not limited access.

S3(simple storage service) --> Objective storage equals to google drive

s3 Glacier --> its is chepaer than all storage volumes its we can use for long storage

